# Core
pydantic>=2.7.0
structlog>=24.1.0

# Video I/O
ffmpeg-python>=0.2.0
opencv-python-headless>=4.9.0

# ML / CV
ultralytics>=8.2.0          # YOLOv8 / YOLOv9 (AGPL — used at training/export time)
onnxruntime>=1.18.0         # Apache 2.0 inference path for exported .onnx models
torch>=2.2.0
torchvision>=0.17.0

# HuggingFace Hub (model downloads + publishing)
huggingface_hub>=0.24.0

# HTTP (vLLM client)
httpx>=0.27.0

# Utilities
tenacity>=8.3.0             # retry on transient errors
numpy>=1.26.0
pillow>=10.0.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
